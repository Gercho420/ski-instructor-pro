DATABASE_URL=mysql://user:password@localhost:3306/skipro
JWT_SECRET=super_secret_jwt_key_at_least_32_characters_long
PORT=5000

# Dominio de producción, SIN barra final (usado en canonical, Open Graph y
# JSON-LD dentro de client/index.html vía reemplazo nativo de Vite %VITE_SITE_URL%).
# Reemplazar por el dominio real en Railway apenas esté definido.
VITE_SITE_URL=https://skipro.example.com
